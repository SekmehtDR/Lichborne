# CLAUDE.md

AI-facing project guide. Loaded into every Claude session for this repo — keep tight, keep operational, keep current. **If you make a non-trivial architectural change, update this file.** The goal is that a fresh session can pick up the project without re-deriving its conventions from the code.

This file is for *behavior* (what to do / not do, where to look, what not to violate). For *how things work*, link into [DESIGN.md](DESIGN.md). For *what changed*, see [Tracker.md](Tracker.md).

---

## What this is

Lichborne is a DragonRealms (text MUD) game client. Electron + React + TypeScript. Connects via Lich5 (recommended) or direct SGE login. Windows x64 only. Single-developer project with a small, reachable tester pool.

**Product position (DESIGN.md §1, §24):** **display and configuration layer over Lich**, not a Lich-replacement. Lich owns automation (scripts, variables, triggers, repository). Lichborne owns rendering, themes, the panel system, profile portability, and surfacing Lich state. **Push back on requests that head toward duplicating Lich functionality** — say so before implementing.

---

## Principles (rules of the road)

1. **YAML is truth for user state. localStorage is a working copy.** New per-character settings need only a `scopedKey(character, ...)` write + `saveProfile()` call. Shared settings live in `_shared.yaml` and need typed entries in `SharedProfile` + `buildSharedProfile` + `importSharedProfile`.
2. **Lich is optional but recommended.** Every feature must work without Lich; Lich integration is additive.
3. **No silent data loss.** If a file can't be processed (future-version YAML, malformed XML), log it and **preserve it on disk**. Never overwrite content the code doesn't understand.
4. **Theme any new UI.** All colors come from CSS custom properties in `themes.ts`. Use `color-mix(in srgb, var(--x) N%, transparent)` for tints. Check the light themes (Ivory, Mist, Parchment) and Terminal.
5. **Stream IDs preserve case end-to-end.** A Lich script that pushes to `LichScripts` lands in a panel called `LichScripts`. The `NEVER_DISCOVER` filter is the only case-insensitive check.
6. **Per-session everything.** Every IPC payload carries `sessionId`; every listener filters by it; every per-character state lives under `lichborne.{character}.{suffix}`. Cross-session bleed is the most common bug class.
7. **Fix root causes, not symptoms.** Past root-cause fixes (B14, B17, B20, B33, B36, B53, etc.) left inline comments explaining the current shape of the code. Read them before patching nearby logic.
8. **Ship reversibly.** Bold changes welcome (v0.6.3 maps rewrite, login redesign). Pair them with migration paths (profile schema versioning, NSIS rescue hook) so a bad rev never costs a tester their data.

---

## Architecture (pointers, not duplication)

### Process split
- **`src/main/`** — Electron main (Node). Sockets, XML parsing, Lich child process, file I/O. Built with esbuild (`build-main.mjs`).
- **`src/renderer/`** — React UI. Built with Vite. Talks to main only via `window.api.*` (preload.ts contextBridge).
- **`src/shared/types.ts`** — single source of truth for IPC payloads and game-event shapes. Imported by both sides.

### Multi-session model (DESIGN.md §13)
One window, N character sessions. `src/main/main.ts` holds `sessions = Map<SessionId, Session>`; each Session owns its own `ConnectionManager`, `StormFrontParser`, `LichBridge`. Every push channel (`game-event`, `connection-status`, `raw-xml`, `error`) carries `sessionId`. **When adding any IPC, include `sessionId` in the payload.**

### Lich launch & connect (v0.7.0; GTK-friendly spawn v0.9.x)
`LichConnection.launch()` spawns `rubyw lich.rbw --stormfront --dragonrealms` as a **detached** child (not a service). Lich takes its force-mode path: bind `127.0.0.1:11024`, then `accept` **exactly one** front-end and **close the listener** — one Lich = one session. Readiness is **not** a fixed delay: `connectWithRetry()` retries the real connection (250ms cadence, ≥30s cap) until the port accepts — the first success IS the session socket (no throwaway probes). `launch()` resolves on the child's `spawn` event and watches `exit` so a Lich that dies on startup fails fast (with its launch-log tail). Because each Lich serves one front-end then frees 11024, multi-character logins **must serialize** their spawn→connect window — `serializeLichLaunch()` in [ConnectionManager.ts](src/main/connection/ConnectionManager.ts) is a module-level promise chain shared across all per-session managers. SGE/eaccess auth runs outside that chain so it overlaps the queue wait. A failed launch `killProcess()`es its Lich so it can't squat the port.

**Spawn shape is GTK-friendly (v0.9.x).** Lich runs as a normal Windows GUI-subsystem process so Ruby/GTK scripts get a proper process/desktop context — the community-standard shape (matches how Frostbite / Genie launch Lich). Three things make it so, all in `launch()`: (1) it runs **`rubyw.exe`** (GUI subsystem), not `ruby.exe` (console) — `resolveRubyw()` derives `rubyw.exe` from the configured ruby path (`/ruby\.exe$/i` → `rubyw.exe`), falling back to the given path if the derived file doesn't exist (so a path already pointing at `rubyw.exe` is used verbatim); (2) **no `windowsHide`** (rubyw is windowless by nature — nothing to hide); (3) **no stderr pipe** — Lich's stdout+stderr are redirected to a per-character log file at `{userData}/Logs/lich-launch/{Character}.log` (truncated per launch), and `describeExit()` reads that file's tail for the error banner instead of an in-memory pipe. The redirected-console **pipe** of the old shape was the prime suspect for GTK flakiness (the kind of non-GUI process context that made widget pumps racy), which is why diagnostics moved to a file. `detached` + `unref()` + the child handle are kept (Lich is the proxy, must outlive us; the handle lets `exitInfo` fail a dead launch fast). The settings/default ruby path stays `ruby.exe` ([lichSettings.ts](src/renderer/lichSettings.ts)) — derivation handles rubyw at launch time, no migration.

**Ruby GTK script windows — product stance REVERSED (v0.9.x), verification pending.** The prior decision was "do NOT support GTK" (2026-05-30/06-01), on the theory that Lichborne's launch shape made GTK irredeemably flaky. **That decision is reversed** (Sekmeht, "I need to revisit the connection stuff to allow for GTK — it's the only way"). The v0.9.x **GTK-friendly spawn shape** (above: `rubyw.exe`, no `windowsHide`, no stderr pipe) was changed specifically to give Ruby/GTK a normal GUI-subsystem process context — the same shape Frostbite/Genie use, under which the same scripts work. **Status: expected to fix the GTK flakiness, pending empirical verification** (start `;vars setup` / `kill-counter` and confirm the window paints + survives interaction). Until that's confirmed in testing, treat GTK support as "should work now" rather than "guaranteed."

The historical root-cause analysis (still useful background): GTK scripts (`Gtk.queue { ... }` + `Gtk::Window.new`) showed (a) windows that failed to paint, (b) windows that painted only **intermittently** (kill-counter often surfaced only after a start→kill→restart cycle — timing-dependent, not categorically "never paints"), and (c) crashes on widget interaction (the `;vars setup` shape — a window that *did* paint could still take Lich down on interaction). The loop itself WAS running (`lich.rbw:135` calls `Gtk.main` unconditionally; `gtk.rb:148-155` yields via `GLib::Idle.add`), so the failure was a subtler process-context problem — which the old console-subsystem (`ruby.exe`) + hidden-window + redirected-stderr-pipe spawn produced, and which the new GUI-subsystem (`rubyw.exe`) plain spawn removes. **When a tester reports a GTK script not working:** first confirm they're on a v0.9.x+ build (the new spawn); if it still misbehaves, that's now a real bug worth investigating (capture the launch log at `{userData}/Logs/lich-launch/`), not expected behaviour. The `<streamWindow>` / `<pushStream>` XML approach (see `newkill-counter.lic`) remains the more portable recommendation for script authors (works in every front-end with no GTK runtime), and Lichborne-native equivalents are still preferred where they exist (`;vars setup` → the Lich Dashboard → Variables editor, v0.9.0). **The v0.9.0 proactive `--- Lichborne:` GTK advisory (the GTK detector + `client-notice` channel) was REMOVED in v0.9.x** alongside this launch change — it assumed GTK is broken, which is no longer the operating assumption (see the #54 tombstone). Pitfall #52 (the `;vars setup` GTK root-cause analysis) is kept as useful background. Pitfall #53 (write-through-Lich editor pattern) is unaffected.

### Game-text pipeline
`SGEConnection`/`LichConnection` → `ConnectionManager` (line events) → `LichBridge.interceptLine` → `StormFrontParser.parse` (stateful XML tokenizer) → `GameEvent[]` → batched IPC → `GameWindow`.

`StormFrontParser` is **stateful** (boldDepth, color stack, monoMode, injuryBuf, etc.). Call `parser.reset()` on new login. `<prompt>` is a good boundary for transient state.

Main text rendering uses **react-virtuoso**. Scroll pinning is a delicate state machine: `pinnedRef` snapshots before commit, `suppressUntilRef` gates programmatic scrolls (200ms; `scrollToBottom` 300ms), `onWheel` un-pins synchronously. Scroll-to-bottom is **instant** (`followOutput` returns `'auto'`). A story-window smooth-scroll feature existed v0.6.8–v0.6.11 (opt-in setting + flood-adaptive fallback) and was **removed in v0.6.12** — marginal, and a magnet for false bug reports. **Don't add `useEffect`-driven scroll logic** (or re-add smooth scroll) without reading GameWindow.tsx and DESIGN.md §23. Room-state updates funnel through the rAF pump (`roomQueueRef`), not direct `setRoomState` — see DESIGN.md §23 and pitfall #20.

### Profile system (DESIGN.md §20)
YAML files in `app.getPath('userData')/profiles/` — `%APPDATA%\lichborne\profiles\` in production (lowercase — `app.getName()` reads the package.json `name` field, NOT `build.productName`). `ensureProfilesDir()` in [profiles.ts](src/main/profiles.ts) is the canonical accessor. Two-stage migration (NSIS `preInit` + runtime fallback) rescues pre-v0.6.4 install-dir profiles.

- **Per-character setting** — write to `scopedKey(character, 'myThing')` and call `saveProfile()`. The dynamic `state:` pipeline picks it up automatically. No type changes needed.
- **Shared setting** — must be added to `SharedProfile` type + `buildSharedProfile` + `importSharedProfile`.
- **Schema versioning** — both files declare `profileVersion`. Migrations registered in `profile-migrations.ts` by SOURCE version. Adding optional fields is NOT a breaking change. Future-version YAMLs are skipped (preserved on disk).
- **Backups** — timestamped `{name}.yaml.{ISO-ish}.bak`, rolling retention of 5.

### Map system (DESIGN.md §19)
Two views: **Lich Map** (Lich image tiles) and **Genie Maps** (Genie XML-driven graph). Genie XML is the spatial source of truth for the graph view — coordinates come directly from the hand-curated XML files; no auto-layout, no BFS placement, no zone stitching. One zone visible at a time, picked by the dropdown or auto-switched when the player's room title matches a room in another loaded zone.

**Two prior approaches have been deleted; do not reintroduce.** The per-zone Genie Graph (`MapGraphView.tsx`, deleted v0.6.3) tried to render zones individually with the old augmentation matcher. The Lich-native BFS view (`LichGraphView.tsx` + `lichLayout.ts`, deleted v0.6.6) tried to make every Lich room renderable by inferring layout from `wayto` cardinals — but Genie's hand-curated coords and Lich's directional walks disagree in dense clusters, producing "type west, marker goes north" misrenders.

**Rendering matches Genie's `MapForm.cs` exactly.** 8×8 node rects are CENTERED on the XML position because Genie's `ConvertPoint(pos, 4)` SUBTRACTS the offset (MapForm.cs:187–193). Arc endpoints go through the XML position directly. Labels at `(pos + 1, pos + 1)` matching `DrawString(r.X + 1, r.Y + 1)`. Getting any of this wrong visibly misaligns label clusters; verify against the reference source instead of guessing.

**Arcs render in two passes** for dense-cluster legibility: under-pass at full opacity (drawn before nodes; lines disappear into rect fills inside clusters) + over-pass at 0.35 opacity drawn on top (faint trace across rects so you can follow a line all the way to its endpoint). Each pass collapses N arcs into 3 `<path>` elements (one per cardinal/climb/go category) via concatenated `M x,y L x,y` segments; same data, render twice, ~6 SVG elements total.

**Cross-zone stubs** are 1-room boundary markers with `note="MapXX_Name.xml"`. `isStubNode(n)` detects `.xml` in note aliases. Title lookup MUST prefer non-stub matches (otherwise the marker in zone A wins over the real room in zone B) — and it merges `byTitle ∪ byNormalized` *before* the non-stub filter, so a `byTitle`-only stub can't trap the match when the real room is reachable through the normalized form (B78).

**Ambiguous-title disambiguation in `currentLocation` (v0.7.0).** Same-titled rooms (the Crossing has 7 "Moonstone Street" rooms) resolve in this order: (1) exact normalized **description** match; (2) **graph adjacency** — the candidate joined by a Genie arc to `prevLocRef` (the last resolved room); (3) file order (`pool[0]`) as last resort. Adjacency is what makes the marker track correctly *while running* — the game streams room titles with no fresh description per step, so without it the matcher fell back to `pool[0]` and the marker stuck on the wrong same-named room until the player typed `look`. `prevLocRef` is updated one step behind by an effect, advanced only on a non-null match.

**Click model (v0.6.8): left-click pins, right-click walks.** Left-click a regular room → toggles a pinned gold BFS path (no walking). Left-click a stub → switches the displayed zone to its target XML. Right-click anything → walks (BFS in-zone, `sendWalkPath`). The map only auto-switches zones when the player's `roomTitle` actually lands in the new zone — never on walk completion (the game can block any command; racing the map ahead strands the user).

**Camera follow-the-player** uses `useLayoutEffect`, not `useEffect`, so the transform update lands in the same paint frame as the indicator's new world position. Default ON; manual pan/zoom turns it off; the ◆ button re-enables. Always-center model (not margin-snap). The follow effect gates on `followNode` — the *same* `visibleById.get(...)` lookup the indicator uses — so "marker visible" and "camera following" can't desync.

**Smooth camera motion.** Pan group is positioned with the CSS `transform` *property* (not the SVG attribute) so `.genie-pan-smooth` can `transition` it (`150ms linear` — linear, not ease-out, because a follow camera re-targets every step and ease-out's velocity reset pulses visibly). `snapTransform` (delta > 600px or > 20% scale vs last-painted transform) drops the transition for big jumps so zone switches / ◆ / fit cut instantly. The indicator carries the *same* transition + snap flag so it stays screen-centred during walks instead of bouncing. Gated on the `mapAnimations` setting (default ON): pan group + indicator only get `.genie-pan-smooth` when `!isDragging && mapAnimations`. (v0.6.8–v0.6.11 gated this on a separate `smoothScroll` setting; v0.6.12 removed that and folded the map glide under `mapAnimations`.)

**Current/selected/hover/hover-path/pinned-path indicators are hoisted OUT of `nodeRects`** as single overlay elements. Pre-hoist, walking re-built the entire `nodeRects` JSX every step. The current-room indicator is a sonar locator: two `genie-here-ping` expanding rings + the solid `INDICATOR_R` ring; the pings are exempt from the animation-pause via a higher-specificity rule.

**Per-color effect system (v0.6.7).** Every COLOR_LEGEND category has its own visual signature (heartbeat for Healer, coin glint for Shop, ripples for Water, leaves for Lumberjacking, dirt for Mining + Trailhead, vortex/drift/rise sparkles for the magical 4, etc.). The structure is uniform — adding a new effect is a 4-step pattern: (1) declare a `*_COLORS` set or extend `MOTE_EFFECTS`, (2) add a memo at the appropriate place in the component that builds the SVG elements per visible-node match, (3) add a CSS keyframe in `map-panel.css`, (4) render the memo at the correct layer in the SVG tree. Stub `↗` glyph always wins over category glyphs (cross-zone identity > resource category). `getMoteContrastColor(hex)` picks mote fills based on room luminance (light bg → dark motes, dark bg → light motes). `normalizeNodeColor()` in `parseGenieZone` converts CSS color names (`"Blue"`, `"Red"`, etc.) to canonical hex so downstream effect lookups don't silently miss rooms.

**Animation pause — two freeze classes (v0.6.7, extended v0.6.9).** The pan group `<g>` carries ONE of two mutually-exclusive freeze classes, both cascading `animation-play-state: paused` to descendants:
- `genie-pan-dragging` — *transient* freeze when `isDragging || inMotion`. `isDragging` flips on mousedown/up; `inMotion` flips true on any `currentLocation` change and resets a `MOTION_QUIET_MS = 800` timer. The locator sonar ping is **exempt** (`.genie-pan-dragging .genie-here-ping` keeps it running) — you still want to see yourself mid-walk.
- `genie-anim-off` — *permanent* freeze when `settings.mapAnimations` is off (default ON). **No ping exemption** — "off" stops the ping too.
The className picks one or the other (`!mapAnimations ? 'genie-anim-off' : (isDragging||inMotion) ? 'genie-pan-dragging' : ''`) so the ping-pause and ping-exemption rules never collide.

**Category effects are OMITTED (not paused) and CAPPED (v0.7.0).** Two layers of defense, both because SVG CSS animations are expensive — each animated element forces Recalculate Style every frame and is a compositor-layer candidate:
- *Omitted during motion.* `animation-play-state: paused` stops an animation advancing but leaves the element layer-promoted — profiling during travel showed residual Layerize ~21% even with effects "frozen". So the 10 animated per-category effect groups (sparkles, heartbeats, coin glints, ripples, bubbles, caution/implode rings, leaf/dirt falls, XP rises) render only when `showEffects = mapAnimations && !isDragging && !inMotion`. During travel/pan/off they're not in the DOM at all. They re-mount once the player has been still for `MOTION_QUIET_MS` (1500ms — long enough that normal sub-second running cadence never re-mounts mid-run; a short window would tear them up and down every step).
- *Capped to the nearest N rooms.* Idle in a dense zone (the Crossing) all effects on was ~29% Recalculate Style. The effect memos iterate `nearbyNodes`, not `visibleNodes` — `EFFECT_CAP` (30) rooms nearest the player (or zone-centre when viewing a zone you're not in), or the stable `EMPTY_NODES` ref when `!showEffects`. So at most ~30 rooms animate regardless of zone density, and the effect memos don't recompute during travel (dep is the unchanging `EMPTY_NODES`). **`auras` deliberately still uses `visibleNodes`** — they're static (no animation, no per-frame cost) and they ARE the colour map.

The freeze classes still cover what *stays* rendered (the sonar ping, the static/fire auras). Node **hover** is likewise suppressed during motion (`onNodeHoverEnter` checks `inMotionRef`) — the map scrolling under a stationary cursor otherwise fires a storm of pointer enter/leave → re-renders.

**`mapAnimations` setting (v0.6.9; sole map-motion switch as of v0.6.12).** `mapAnimations` (default ON) is the single toggle for ALL Genie map motion — the per-color category effects AND the camera glide. Off → `genie-anim-off` freezes effects and the glide is disabled (camera snaps). Per-character `AppSettings` field, persists via the dynamic profile `state:` pipeline, threads `GameWindow → PanelFrame → MapPanel → GenieMapView` (the `autoLinkUrls` path). (v0.6.9 also had a `smoothScroll` setting; removed v0.6.12 with the story-window smooth scroll.)

**Genie parse cache (v0.6.7).** First-time parse of a 122-XML maps folder takes several seconds (DOMParser is synchronous and chunky). `genie-cache:load` / `genie-cache:save` IPC handlers in `main.ts` serialize the parsed `Map<zoneId, GenieZone>` to `userData/genie-cache.json`. Subsequent launches verify a fingerprint (sorted `filename:mtimeMs:size`) against current folder state and skip re-parsing if it matches — load drops from seconds to ~50ms. Version field (`GENIE_CACHE_VERSION`) lets us invalidate every user's cache when the GenieZone shape changes without manual cleanup. Cache write is fire-and-forget after parse.

Files: [MapPanel.tsx](src/renderer/components/panels/MapPanel.tsx), [MapImageView.tsx](src/renderer/components/panels/MapImageView.tsx), [GenieMapView.tsx](src/renderer/components/panels/GenieMapView.tsx), [mapTypes.ts](src/renderer/components/panels/mapTypes.ts).

### Theme system (DESIGN.md §6)
~150 CSS custom properties in `themes.ts`. `darkBase` is the canonical full set; every other theme is a partial override merged over `darkBase` in `applyTheme` / `applyCustomTheme`. Theme Editor (`ThemeEditor.tsx`) uses a `TABS` declarative schema — new variables surface there.

### Automations, groups, modes (DESIGN.md §8)
Highlights, triggers, macros, aliases, and contact templates share a `groupIds: string[]` + `allGroups: boolean` shape. `isRuleActive(rule, activeGroups)` gates them. New rule types should follow this pattern and default to `allGroups: true`.

### Login & character-selection flow (v0.8.0, DESIGN.md §13.6)
Empty state and "+" tab open `Launcher.tsx` + `AddCharacterWizard.tsx`, not `LoginScreen.tsx` (kept only as type-host for `SessionInfo`). `LichSetupFields.tsx` is the single source of truth for Lich path/port/mode editing — three render sites (Launcher dialog, Settings panel, legacy LoginScreen), all must wrap it in `.login-form` class for CSS scoping.

- **Launcher rendering rules** are documented in DESIGN.md §13.6. Key invariants: Favorites is always-open; per-account sections default collapsed but **single-account is always expanded** and **freshly-added accounts auto-expand once** (wizard writes the account name to `lichborne.launcher.expandedAccounts` before `onCompleted` bumps `refreshKey`, and Launcher's refresh effect re-reads it).
- **AddCharacterWizard is account-driven, not character-driven** (v0.8.0). Step 1 collects account/password/game; step 2 runs `eaccessFetchCharacters` (mode-agnostic SimuCo auth, no Lich needed) and bulk-writes one stub `CharacterProfile` per checked discovered character. Existing profiles are listed but disabled. No connection happens in the wizard — tiles appear, user clicks Connect when ready.
- **Per-tile state is launcher-owned and writes immediately** via small read-modify-write helpers (`setCharacterFavorite`, `setCharacterGame`, `setCharacterUseLich`, `setCharacterHidden`, `patchCharacterProfile` for the notes editor). Never go through the GameWindow's debounced `scheduleProfileSave` for these — that's a different ownership track (see Pitfall #26 below).
- **Bulk Connect** ([BulkConnectPicker.tsx](src/renderer/components/BulkConnectPicker.tsx) + `runBulkConnect` in App.tsx) runs sequential per-account connects. DR's one-character-per-account rule forces this; sequential is also the simpler error-isolation model.
- **Same-account conflict** raises a confirmation modal (Cancel / Disconnect-and-continue) instead of refusing. The disconnect uses the awaitable `disconnect-await` IPC; the conflicting tab stays in the bar in disconnected state.
- **Shutdown** uses `gracefulDisconnect({ quickClose: true })` which calls `socket.end()` (guaranteed flush of QUIT) + force-close without waiting for server ack. ~300ms total regardless of session count. In-tab Disconnect and the conflict-modal auto-disconnect keep the full 5s ack-wait — they need the account slot confirmed released for the next action.

### Import wizard (DESIGN.md §15)
Wrayth XML / Genie .cfg / Frostbite .ini parse into a neutral `ImportCandidate` intermediate, then `mapper.ts` converts to native rules with `allGroups: true` and blank names. Per-client quirks under `src/renderer/import/`. Substitutions counted but not imported (F12).

### Session Log (DESIGN.md §28, shipped v0.7.0)
Per-character daily plain-text logs. **Capture** happens in `GameWindow.onGameEvent` → `logToSession()` → `session-log:append` IPC → buffered per-character writer in [sessionLog.ts](src/main/sessionLog.ts). Files: `{userData}/Logs/{Character}/{Character}_YYYY-MM-DD.log`, line format `[HH:MM:SS][stream] text` (the date is in the filename; parsers also accept the legacy `[YYYY-MM-DD HH:MM:SS.mmm]` form). Buffered (1s / 100-record flush, force-flush at 5000); `flushAllSessionLogs()` runs on graceful close next to `backupAllProfiles()`.

- **Config is app-wide, NOT per-character.** All Session Log preferences (capture gates, retention, compression, raw-size cap, the Recent-tail filter, the Export-builder format prefs) live in one `SessionLogSettings` object — [sessionLogSettings.ts](src/renderer/sessionLogSettings.ts), stored in `_shared.yaml` via `SharedProfile.sessionLog`. `logToSession` calls `loadSessionLogSettings()` fresh per batch (it's a tiny localStorage read — no stale-closure ref needed, and a Settings change applies instantly to every open character). The capture config also rides along in `SessionLogAppendPayload` (`retentionDays`/`compress`/`maxRawMB`) so main can run maintenance without its own copy. Both the SettingsPanel section and the modal read-modify-write the shared object so neither clobbers the other's fields.
- **What's skipped:** `raw`, room sub-streams (`ROOM_STREAMS`), blank lines — state, not history. Stream IDs `cmd`/`sys`/`main` are the capture-category keys; everything else counts as "streams".
- **Size management (v0.7.0):** closed (non-today) day-files are gzip-compressed to `.log.gz` (~85-90% smaller) by background `maintain()` — runs at session start + day-rollover, gated by `sessionLogCompress`. All readers route through `readLogFile()` (gunzips transparently) and `resolveDayFile()`. Two retention limits: `pruneOldLogs` (age, `.log` + `.log.gz`) and `enforceRawSizeCap` (caps *uncompressed* `.log` bytes only — never `.gz`, never today's file). If you change the line format or add a maintenance limit, keep both parsers (`LINE_RE` in the modal, `EXPORT_LINE_RE` in main) accepting the legacy form.
- **UI:** [SessionLogModal.tsx](src/renderer/components/SessionLogModal.tsx) — one modal, three views (Recent Tail + Quick Search + Export), opened by the toolbar **Logs** button or right-click → "Show in Log" (which opens straight into Search). Tail is paginated (`PAGE`/`JUMP_RADIUS`); never loads a whole day. The Recent-tail filter (`filterHidden`/`filterDedup`) and the Export format prefs persist in the app-wide `SessionLogSettings`, not per-character.
- **Export builder ("Create Log File"):** the renderer gathers a `SessionLogExportSpec` (date range, stream layers, include-timestamps/tags, dedup, summary, split-per-stream, target) and main does all the reading/filtering/formatting/writing in `session-log:build-export`. Big data never crosses IPC — only the spec and a small `SessionLogExportResult`. `target: 'file'` runs a save (or folder, when `splitPerStream`) dialog; `'clipboard'` writes combined text via Electron `clipboard`.
- **Read IPC:** `session-log:list-days`, `read-day` (tail + `beforeLine` pagination), `list-streams` (single day, or a range when `toDate` is passed), `search` (returns `SessionLogSearchHit` with `lineNo`/`total` for jump-to-tail), `build-export`, `disk-usage`, `open-folder`. All read handlers flush the character's buffer first so reads reflect the live session.

---

## Pitfalls (read before touching adjacent code)

> **Adding a new pitfall — read this first.** Pitfalls are numbered sequentially and the numbers ARE referenced from other comments / commit messages / docs (e.g. "see CLAUDE.md pitfall #33"). When adding a new one, **find the highest-numbered existing pitfall and add the new one AFTER it, with the next number** — do NOT insert before an existing pitfall or renumber an existing one. The natural editing instinct (using Edit with an existing pitfall as the anchor `old_string` and putting the new content first in `new_string`) puts the new pitfall in the wrong position; the correct anchor is the `---` separator at the END of the pitfalls list, with the new pitfall inserted between the last existing pitfall and that separator. Sekmeht caught me making this exact mistake three times across v0.8.7 and v0.8.8 (B121, B124, B126) — each time renumbering an older pitfall instead of just appending. The fix path when caught: revert the renumbered older pitfall to its original number, then append the new one at the end. The numbering is `1, 2, 3, ..., 31, 33, ...` (32 has been missing for ages; don't try to fix the gap, it's referenced from elsewhere by its current number).

1. **`app.getName()` returns `name`, not `productName`.** userData is `%APPDATA%\lichborne\` (lowercase). The `productName: "Lichborne"` under `build:` only affects installer / Start menu display. Mixing the cases caused a v0.6.4 hotfix to silently no-op.
2. **electron-builder NSIS `customInit` runs AFTER the previous uninstaller.** Use `preInit` and read the previous install location from `HKCU\Software\${UNINSTALL_APP_KEY}` (`$INSTDIR` isn't set that early).
3. **NEVER put user data inside the install directory.** NSIS `RMDir /r $INSTDIR` on every upgrade. Pre-v0.6.4 lost everyone's profiles this way.
4. **`StormFrontParser` is stateful and lives for the session lifetime.** Reset on new login. Stream-mode state can bleed across `<prompt>` boundaries.
5. **Scroll pinning fights you if you add naive useEffect logic.** Read GameWindow.tsx + DESIGN.md §23 before adding any programmatic scroll.
6. **Stream IDs are case-sensitive everywhere except `NEVER_DISCOVER`.** Lowercasing them anywhere will break Lich scripts. See B34.
7. **`lichPort` is the Lich front-end port, not an SGE shard port.** Don't look it up from a per-game table at credential build-time (v0.6.3 bug).
8. **`setState` in a mousemove handler must be gated on a hover sentinel** or you re-render on every pixel. See `GenieMapView.tsx` `onMouseMove` (gates on `dragRef.current`).
9. **Genie node rects are CENTERED on the XML position, not top-left anchored.** `ConvertPoint(pos, 4 * scale)` SUBTRACTS the offset (MapForm.cs:187–193). Anchoring top-left shifts every node 4px down-right and misaligns label clusters. Verify against Genie source for any map-rendering math change.
10. **`pointer-events: none` on the SVG pan group breaks click-to-walk.** `isDragging` flips true on mousedown BEFORE click fires — toggling `pointer-events` on the parent at that point makes the click target the SVG root, not the inner node `<g>`. Gate hover at the React layer (check `dragRef.current` in the hover handler) instead.
11. **Use `useLayoutEffect` for camera-follow.** The map's follow-the-player effect must update the transform in the same paint frame as the indicator's new world position. Plain `useEffect` produces a 1-frame "indicator at new spot, camera still old" flash at high walk rates.
12. **`useRef(initialValue)` initialized to a meaningful prop is a footgun.** The "did this change since last render" pattern requires `useRef(null)` (or whatever sentinel ≠ the first real value) so the first non-null render is treated as a change. See `lastLocationRef` in GenieMapView for the cautionary tale.
13. **Genie XML colors can be CSS color names, not just hex.** Some maps use `color="Blue"`, `"Red"`, `"Aqua"`, `"Lime"`, `"White"`. SVG `fill` accepts both, but `COLOR_LEGEND` and every effect lookup keys by hex. `normalizeNodeColor()` in `parseGenieZone` converts these to canonical hex at parse time so downstream code sees a single form. If a new room mysteriously has no aura or animation, check whether its color attribute is a named color we don't normalize.
14. **`opacity` SVG attribute conflicts with CSS animation on `opacity`.** Per SVG2 spec CSS wins, but having both set creates ambiguity that browsers can handle inconsistently. When applying an animated class to an SVG element, omit the `opacity` attribute (set to `undefined`) so CSS owns it cleanly. See the aura render's `opacityAttr` ternary.
15. **`indicator.id` is case-normalized to lowercase by the parser** (`raw.replace(/^Icon/i, '').toLowerCase()`) for the typed-flag checks AND the generic event payload. Renderer keys (`indicators.poisoned`, `indicators.bleeding`, etc.) are lowercase. Don't switch on `IconBLEEDING` directly downstream — match the lowercase form.
16. **If you change the `GenieZone` / `GenieNode` / `GenieArc` shape, bump `GENIE_CACHE_VERSION`** in `main.ts`. The parse cache stores the entire serialized zone graph; a shape change without a version bump means old caches deserialize into wrongly-shaped objects and produce silent runtime errors deep in the renderer. The version bump invalidates every user's cache on next launch with no manual cleanup.
17. **Map animations pause via the parent `<g>`'s class, not per-element.** `.genie-pan-dragging *` cascades `animation-play-state: paused` to all descendants. Don't add `pointer-events: none` to that group when toggling drag/motion — it would shift the click target off the inner node `<g>` and silently break click-to-walk (the mousedown sets the class before click fires).
18. **The map pan group and the current-room indicator must transition in LOCKSTEP.** Both carry `.genie-pan-smooth` (same `150ms linear`) and share the `snapTransform` flag. The indicator lives inside the pan group, so its screen position is `pan ∘ indicator`; if only the pan transitions while the indicator's world position jumps instantly, the halo bounces off-centre every walk step. Matched transitions make the interpolations cancel. Don't change one easing/duration without the other.
19. **No inline `height: 100%` on the `GenieMapView` outer wrap.** It's a flex child (`.map-canvas-wrap` → `flex: 1; min-height: 0`). An inline `height: 100%` overrides flex sizing and, evaluated against the parent's full height before the parent subtracts its own toolbar, pushes the MapPanel's view-selector toolbar off-screen at narrow window heights. Keep it `display: flex; flex-direction: column; minHeight: 0` and let CSS own the height.
20. **Room-state updates go through the rAF pump, not direct `setRoomState`.** Fast running emits multiple `room-title` events per frame; React 18 auto-batches them so only the last room survives and the map skips rooms. `roomQueueRef` + a `requestAnimationFrame` loop apply one per frame (cap 8). Don't bypass it.
21. **`Home`/`End` are native (command-box cursor) when the command input is focused.** Only `Ctrl+Home`/`Ctrl+End` and the focus-elsewhere case scroll the story window. `PageUp`/`PageDown` always scroll. Testers expect text-editing keys to edit text (B77). Don't re-claim plain `Home`/`End` for scrolling.
22. **`LichBridge.interceptLine` suppression is poll-aware — don't make it match on output format alone.** The auto-poll's `;listall` response is consumed (hidden) so it doesn't spam the game window, but a player-typed `;list`/`;listall` produces the same format. `LichBridge.pollScriptList()` arms a 4s window; `interceptLine` consumes a matching line only while armed, otherwise lets it through (B79). Route the auto-poll through `pollScriptList()`, never `injector.pollScriptList()` directly.
23. **Never put CSS `contain` on `.text-line` / `.text-line-wrap` (the react-virtuoso rows).** Layout containment isolates each row from Virtuoso's own item-measurement / scroll-offset bookkeeping, so the scroll position can't be held stable when the list updates while the user is scrolled up — what they're reading gets dragged off-screen (B84). It was a v0.6.8 per-row perf hint, removed v0.6.12. The CSS carries DO-NOT-RE-ADD comments.
24. **An inactive character's GameWindow is `display:none` — its DOM measures 0×0.** Every inactive tab stays mounted and keeps processing game events (room titles, etc.), but `clientWidth`/`clientHeight`/`getBoundingClientRect` all read **0** until the tab is shown. Any layout-dependent logic must (a) guard against 0 dimensions so it doesn't write garbage (`centerOnCurrent`, `fitToView`, the follow `useLayoutEffect` all bail on `!w||!h`), and (b) re-run when the tab becomes visible again — GenieMapView uses a `ResizeObserver` on the SVG to recenter the camera on the 0→size transition. Without (b), a character that travels while its tab is hidden tabs back with the camera stranded rooms away. This is the canonical multi-character map pitfall: hidden ≠ unmounted, but hidden = unmeasurable.
25. **`LoginCredentials` must carry per-shard fields; main has no other source for which DragonRealms shard a connect is for.** Lich and SGE both need to know the shard (DR/DRX/DRT/DRF) — Lich because the launch flags (`--dragonrealms` vs `--test --dragonrealms` vs `--platinum --dragonrealms` vs `--fallen`) decide which front-end port it opens, SGE because the gameCode third arg to `authenticate()` decides which character list / login key it returns. Before v0.8.0, `LoginCredentials` had no `game` field, `App.tsx#runConnect` used the *global* `adv.lichPort` instead of the per-character port, `LichConnection.launch` hardcoded `'--dragonrealms'`, and `ConnectionManager` never passed gameCode to SGE — a 4-layer chain (B95) where DRT/DRX/DRF characters silently routed to DR at every layer. Rule now: every per-shard connection parameter rides in `LoginCredentials` — `game`, `lichArguments`, and `lichPort` (derived per-character from the canonical `GAMES` table in [lichSettings.ts](src/renderer/lichSettings.ts) via `gameOptionByCode()`, NOT taken from the global advanced settings). When adding a new shard or a new per-shard parameter, update both `LoginCredentials` and `GAMES`; don't keep a duplicate table in main.
26. **`CharacterProfile` has two ownership tracks; the GameWindow's debounced save must NOT clobber launcher-owned fields.** GameWindow owns `theme` + `state` (the dynamic localStorage-mirror map). Launcher owns `game`, `useLich`, `hidden`, `favorite`, `guild`, `circle`, `notes` — all written via the small read-modify-write helpers in [Launcher.tsx](src/renderer/components/Launcher.tsx) (`setCharacterFavorite`, `setCharacterHidden`, etc.) directly via `window.api.writeCharacterProfile`. Before v0.8.0, `exportCharacterProfile` ([profile.ts](src/renderer/profile.ts)) called `buildCharacterProfile` and wrote the result verbatim — which only knew the GameWindow's fields. Every debounced save from an active GameWindow (firing 2.5s after any per-character settings change) silently stripped favorite/hidden/notes/guild/circle AND reverted game/useLich to the stale session.* values, un-favoriting connected characters and bouncing back launcher-side pill changes (B97). `exportCharacterProfile` now does a **read-merge-write**: spread `built` first, then override launcher-owned fields from the existing YAML so they persist. When adding a new launcher-owned profile field: (a) add it to the `CharacterProfile` type as optional; (b) add it to the merge in `exportCharacterProfile`; (c) write it via a launcher helper, not through any GameWindow path. Don't try to extend `buildCharacterProfile` to know about launcher fields — they're authored from a different surface and the GameWindow doesn't have authoritative values for them.
27. **`discoveredStreams` must never contain a builtin `PanelType` id.** A "discovered" stream is one the game emits via `stream-declare` / `stream-push` events; the renderer adds it to the `discoveredStreams` array so the Panel Manager can surface it as a Custom row in Available Streams. But every builtin PanelType (`combat`, `room`, `exp`, `thoughts`, …) is ALSO offered as a built-in row from `ALL_PANEL_TYPES`. If the same id appears in both lists, PanelManager shows the stream twice — once as a builtin (correctly hidden after add via the `openTypes` filter) and once as a "custom" discovered row (the old `availableCustom` filter only checked `openCustomIds`, and a builtin tab has `type: 'combat'`, not `'custom'`, so the discovered row never hid). Clicking the second row added a duplicate tab with the same id (B100, combat). Root fix is at the discovery filter in [GameWindow.tsx](src/renderer/components/GameWindow.tsx): reject any id matching an entry in `ALL_PANEL_TYPES` in addition to the hand-curated `NEVER_DISCOVER` list — adding a new builtin PanelType is then automatically covered, no `NEVER_DISCOVER` bookkeeping needed. PanelManager's `availableCustom` filter also defensively excludes builtin-type ids and ids matching an open builtin tab, so a stale discoveredStreams from an older session can't reintroduce the duplicate. **When adding a new builtin PanelType, you don't need to touch `NEVER_DISCOVER`** — only add aliases the server might use (e.g. `experience` → `exp`) if any.
28. **Never call `navigator.clipboard.writeText` from the renderer. Always use `window.api.writeClipboard`.** The renderer's `setPermissionRequestHandler` / `setPermissionCheckHandler` (set up originally for the font picker's `queryLocalFonts`) refuse `navigator.clipboard.writeText` silently — Electron's internal permission name doesn't match a whitelist and the call returns a swallowed promise rejection. No error, no log, nothing reaches the system clipboard. This was B18 (auto-copy) in v0.1.9 and re-bit us as B102 (Debug Copy All) in v0.8.2 because DebugPanel was the one site that never got migrated. The fix every time is the same: route through `window.api.writeClipboard` → `write-clipboard` IPC → main's `clipboard.writeText` (which has no permission guard). When adding any new "copy to clipboard" UI, use the IPC channel from the start. Grep `navigator.clipboard` periodically — if a hit is in renderer code, it's a bug.
29. **Highlight / trigger regexes are compiled with `/g`; loops using `regex.exec()` must `lastIndex++` after a zero-width match.** Highlights compile via `new RegExp(...source, rule.caseSensitive ? 'g' : 'gi')` in [highlights.ts](src/renderer/highlights.ts). When walking matches with `while ((m = regex.exec(text)) !== null)`, a zero-width match (e.g. user pattern with an empty alternative like `(word1|word2|)`) leaves `lastIndex` at the same position and the loop spins forever. Both the renderer (`renderSegmentFull.tsx`) and the Fires log (`logHighlightFiresRef` in GameWindow) have this guard — copy the pattern when adding any new exec-loop. Also: don't use `regex.test()` if you want "does it match a NON-empty string" — `test()` returns true for empty matches. The Fires log used to use `test()` and logged phantom fires for malformed patterns until B101 (v0.8.2) replaced it with `exec()` + length check.
30. **Don't `encodeURIComponent` the `URL=` param of `https://www.play.net/bounce/redirect.asp`.** play.net's bounce takes the value after `URL=` *literally* — Genie's `FormMain.cs` LinkClicked path does raw string concatenation for exactly this reason. Encoding `://` to `%3A%2F%2F` makes the bounce page hand the browser a malformed destination ("site doesn't exist"). The F23 wrapper in [renderSegment.tsx](src/renderer/utils/renderSegment.tsx) ships intentionally raw (B105). The instinct on review is "URLs in query strings should be encoded" — that's the right instinct in general but wrong for this specific endpoint. Comment in the source explains why; preserve it. If the bounce's behaviour ever changes (it accepts encoded input), test by clicking a real external link, not by reading the resulting string.

31. **`dispatchUserText` is the canonical "process text as if the user typed it" path; don't bypass it with raw `window.api.sendCommand` when you want alias resolution + echo + history + log.** Extracted from `handleCommand` in [GameWindow.tsx](src/renderer/components/GameWindow.tsx) as part of v0.8.3 F07 (repeat-command macros). The two callers are (a) the command-bar Enter handler — `{ pushToHistory: true, clearInput: true }`, and (b) the macro token expansion site (`{RepeatLast}` / `{RepeatSecondToLast}` / `{ReturnOrRepeatLast}`) — `pushToHistory` and `clearInput` vary by token. Macro tokens are resolved at **fire time**, not interpolation time, because they reference live state (`historyRef.current` and `commandRef.current`) that doesn't fit into the string-substitution `interpolate(template, vars)` shape. The global keydown handler mounts once with empty deps, so it reaches `dispatchUserText` via `dispatchUserTextRef.current` (latest-instance mirror updated in a no-dep `useEffect`) and reaches the live command bar via `commandRef.current` (mirror of the `command` state, updated when state changes). If you add a new flow that programmatically sends user-equivalent input — a hotkey, a context menu "send", a script-driven replay — route it through `dispatchUserText` so it picks up aliases, the command echo, the session log, and the proper history-push policy automatically. `historyRef` push rule: only the bar's normal Enter and `{ReturnOrRepeatLast}` (typed case) push to history — RepeatLast/SecondToLast must NOT push when replaying, otherwise two presses of RepeatLast would re-pin the previous command and break the next RepeatSecondToLast. Two-press idempotence is part of the Stormfront/Wrayth contract testers expect.

33. **Accessibility overlays (high contrast, color blind) live on top of theme vars — never write theme vars without re-applying overlays.** `applySettingsToDOM` in [settings.ts](src/renderer/settings.ts) writes the active character's accessibility overlay vars *after* the theme vars (e.g. deuteranopia rewrites `--vital-health-ok-start` to a teal). The two writes share keys, so any `applyTheme` / `applyCustomTheme` call from any caller would silently erase the overlay if not followed by an overlay re-apply (B114). The fix is a `registerThemeAppliedHook(fn)` export in [themes.ts](src/renderer/themes.ts): `applyTheme` and `applyCustomTheme` call the hook after writing their vars. GameWindow's theme-apply effect registers a callback that closes over the active session's `settings` and re-runs `applySettingsToDOM(settings)` — so theme writes from ThemeEditor live editing, ThemePicker preview paths, and theme switching all get overlays re-applied automatically. Cleanup on effect teardown clears the hook so an unmounting GameWindow doesn't apply the wrong character's overlays after the next mount. Settings.ts can't import themes.ts directly without a circular dep, hence the registration pattern. When adding a new accessibility-style overlay (epilepsy-safe shapes, dyslexia-friendly font, etc.), add it to `applySettingsToDOM` — the hook will re-apply it after every theme write for free. Don't write overlay vars from outside `applySettingsToDOM` or you'll miss the post-theme re-apply path.

34. **Theme vars: panel-specific colors should cascade from the closest general `--*` var via `var(...)` in `darkBase`, not literal hex.** Established by B108 (`--map-bg` → `var(--bg-app)`) and broadened in B112 (8 panel text vars → `var(--text-secondary/muted/dim)`). The Theme Editor only exposes the *general* surface vars (App background, Game text, Labels, Muted text, etc.); custom themes built via the editor never set the specialty vars. With literal hex defaults in `darkBase`, custom themes were stuck with whatever value `darkBase` baked in — Rakkor's "Game Text" black on a light theme left Room / Hands / Exp panels stranded grey. Pattern when adding any new panel-specific text/bg/border var: pick the closest general var that matches the role (body text → `--text-primary` or `--text-muted`; section labels → `--text-secondary` or `--text-dim`; panel bg → `--bg-base`; panel surface bg → `--bg-app`) and default to `var(--general)`. Built-in themes (Parchment, Mist, Ruby, …) that need a distinct color *still* set the specialty var explicitly — `applyTheme` merges over `darkBase`, so an override always wins. Held / active accent colors (`--hand-held-color`, `--spell-active-color`, etc.) are the exception — they ARE the visual cue and should stay specialty-tinted. If you find yourself writing `'--new-thing-color': '#888888'` in `darkBase` and `#888` is also the value of `--text-muted`, that's the smell: write `'var(--text-muted)'` instead. If you find yourself adding a new specialty var to the Theme Editor schema *just so* light-theme users can override it, that's also the smell — make the cascade do the work.

35. **Built-in themes can share a display `name`; persistence + lookup keys on `id` only.** Established in v0.8.6 F35: both `{ id: 'classic', ... }` (dark) and `{ id: 'classic-light', ... }` (light) ship with `name: 'Classic'`. The two rows in the Theme Picker differentiate visually via the swatch dot (`item.swatches[0]` = black for the dark Classic, white for the light one). ThemePicker uses `item.id` for React keys ([ThemePicker.tsx](src/renderer/components/ThemePicker.tsx) `key={item.id}`) so no double-key warning. `initTheme` reads `localStorage.getItem('lichborne.theme')` (a saved id) and resolves via `THEMES.find(t => t.id === savedId)` — the display-name collision is never load-bearing. If a future contributor sees "two Classic" in the catalog and tries to rename one, they'll break the muscle-memory of testers who picked "Classic" expecting the white-bubble light theme. Keep the duplicate display name when the visual differentiation (swatch dot) is the intended affordance. Apply the same pattern when adding any future paired-theme set (e.g. a Slate Light, Mist Dark).

36. **Contact tracking has TWO long-running effects (last-seen + time-spent polling) that BOTH share the same `pendingContactsRef` buffer — every read AND every state-change must respect it.** Established in B119 + F34. The last-seen effect (fires on `roomState.players` change) builds a 2-second debounced buffer in `pendingContactsRef` to batch lastSeen / lastRoom / encounterCount updates. The 60s `timeSpentMs` polling tick reads contacts to add 60s to anyone in the room. Both effects MUST start from `pendingContactsRef.current ?? contactsRef.current` — using `contactsRef.current` alone misses in-flight buffer updates and the next `saveContacts` clobbers them. The buffer also gets cleared whenever `contacts` state changes externally (ContactsPanel save, import, polling-tick flush): a no-deps `useEffect([contacts])` nulls `pendingContactsRef` + `clearTimeout(lastSeenTimerRef)` — without this clearing, an in-flight buffer would write a stale snapshot back to localStorage and wipe whatever the user just added (B119: Rakkor's "save 12th contact, it disappears"). When adding a new contact-tracking effect, always: (a) base on `pendingContactsRef.current ?? contactsRef.current`, (b) write through `saveContacts` + `setContacts` + `scheduleProfileSave` together, (c) trust the B119 cleanup useEffect to drop your buffer when state changes externally. Don't write to `contactsRef.current` directly — it's a downstream mirror, not a source of truth.

37. **Encounter / presence detection needs TWO gates, not one — absence-threshold AND cooldown.** F34's `encounterCount` increments only when a contact is detected AND `(now - lastSeen) > ABSENCE_THRESHOLD_MS` AND `(now - lastEncounterAt) > ENCOUNTER_COOLDOWN_MS`. The absence-threshold (90s, > polling-tick 60s with a margin) gates "they actually LEFT since last seen" — without it, a contact who stays in your room for 10+ minutes ticks the counter every time `room.players` changes (someone else enters / leaves) because the last-encounter timestamp is old. The cooldown (10 min) gates "even if they left, don't double-count a quick exit-then-re-enter." `lastEncounterAt` updates only on actual increments; `lastSeen` updates on every detection. Both timestamps are needed and serve different purposes — don't fold them. When adding any new contact-state-transition feature (e.g. "first seen on day X" badges), think in terms of presence-edge detection rather than just "is this contact here right now," and use the same absence-threshold + cooldown shape so we don't end up with multiple half-correct definitions of "encountered."

38. **`lichborne:open-quick-send` custom DOM event is the canonical way to open QuickSend from anywhere outside AppShell.** Established in F33 v0.8.6. AppShell owns the QuickSend modal state and listens for the event; on receipt it queries the visible `.session-shell:not(.session-shell--hidden) .command-input` for the value and opens QuickSend prefilled — the same DOM read the keyboard handler does for Ctrl+Shift+Enter. Use the event rather than prop-drilling an `onOpenQuickSend` callback through every GameWindow / panel — AppShell is already DOM-aware for this path, and the event keeps the source-of-input logic in one place. If you add a new "send a command somewhere else" entry point (a context menu item, a panel button, etc.), dispatch `document.dispatchEvent(new CustomEvent('lichborne:open-quick-send'))`. Don't dispatch the event with a payload — the event handler's contract is "open with whatever's in the active command bar" and adding payload-based behavior would split that contract.

39. **Panel Manager has two ownership dimensions; don't conflate "slot added" with "slot has streams".** v0.8.1 introduced explicit add/remove of panel locations — the 4 slots (Main-Top, Top-Right, Middle-Right, Bottom-Right) each carry an `*Added` boolean (`mainTopAdded` / `topAdded` / `midAdded` / `bottomAdded`) that controls whether the slot renders in the game window. The slot's `tabs` array controls which streams live in it. The two are independent. An added slot with empty tabs renders an `EmptyPanelSlot` placeholder; a removed slot can still have tabs in the array — *that's not necessarily a bug*. `removePanelZone` clears the tabs when the user explicitly removes, but the **migration default for `mainTopTabs` was `[]` as of v0.8.3** (was `[room, combat]` in v0.8.1–v0.8.2) precisely because the default + `mainTopAdded=false` produced "phantom tabs in an un-added zone" — invisible to the user but visible to any code that aggregates tabs across zones. **Anywhere code aggregates tabs across the four zones, it MUST gate on the `*Added` flag.** Two sites currently do this (both fixed in B106/B107): (a) `watchedStreamsRef` in [GameWindow.tsx](src/renderer/components/GameWindow.tsx) — used by `STREAM_FALLBACK` routing; without the gate, phantom tabs claim their streams as "watched" and lines like combat / conversations silently buffer instead of falling back to main. (b) `allTabs` in [PanelManager.tsx](src/renderer/components/PanelManager.tsx) — used by `openCustomIds` and `openTypes`; without the gate, phantom tabs hide discovered streams from Available Streams, leaving the user with no path to re-add a stream to a visible slot (this was Legiro's moonwatch / Moons bug). New code that mutates tabs (`addToZone`, `moveTabToZone`) auto-flips the `*Added` flag on as a safety so adding a stream to a removed slot doesn't leave it invisible. Migration defaults when the flag is missing: **Main-Top → false** (new zone, explicit opt-in), other three → **true** (preserve v0.8.0 always-visible). When adding a fifth panel location in the future, follow the same pattern: new `*Added` state + scopedKey persistence + migration default + render-gate in GameWindow + `addPanelZone`/`removePanelZone` handlers + PanelManager UI entry + **audit every cross-zone aggregation site to gate on the new flag**.

40. **`newDiscovered` in GameWindow's event batch must dedupe WITHIN the batch, not just against existing state.** Established in B123 (v0.8.7). The event loop in [GameWindow.tsx](src/renderer/components/GameWindow.tsx) translates every incoming `stream-declare` AND `stream-push` event into a push to a per-batch `newDiscovered: string[]`. The dedup filter at the `setDiscoveredStreams` site checks `existing.has(id)` (against pre-batch state) but originally did NOT track ids it had already added in the same pass. A Lich script that emits `<streamWindow id='X'/>` immediately followed by `<pushStream id='X'/>` produces both events in a single batch; both push `'X'` into `newDiscovered`; both pass the `existing` check (since `X` isn't in `discoveredStreams` yet); both get appended → array becomes `['X', 'X']`. PanelManager renders one row per array entry, so the user sees two identical "Available Streams" rows; adding either makes `openCustomIds.has('X')` true and filters out BOTH rows (this is also how to recognize the bug — adding "one" of two identical-name rows hiding both is a duplicate-id signature, not a display bug). The fix is a `seenInBatch: Set<string>` local to the filter; first entry in a batch passes and gets recorded, duplicates within the same batch are rejected. When adding a new stream-discovery event type in the future (a hypothetical third tag that should also surface a stream), route it through the same `newDiscovered` push site so the existing dedup catches it for free. If you add a separate write site, replicate the `seenInBatch` pattern. The corresponding parser invariant: `stream-declare` (from `<streamWindow>`) and `stream-push` (from `<pushStream>`) deliberately emit independently — script writers' intent is "make sure the UI knows this stream exists" with multiple safety nets, and stripping one of them at the parser layer would break legitimate scripts. Dedup is the renderer's job.

41. **Room sub-stream stale-clear lives at the parser, not the GameWindow event batch.** Established in B121 (v0.8.7). The Room panel's structured sections (`room`, `room-objects`, `room-players`, `room-creatures`, `room-extra`, `room-exits`) get populated from `<component id='room ...'/>` tags. When DR doesn't re-emit a component for a room transition (common — DR is sparse with re-emits unless `LOOK` forces them), the previous room's data carries over and surfaces as ghost players / objects in the Room panel until a manual `LOOK`. **The wrong place to fix this is GameWindow's event batch loop** (which is where I tried first): track which sub-streams "were seen" in this batch, clear the rest on title change. The fatal failure: IPC batches can split title-change from component updates across multiple batches. A batch carrying only the new title with no component re-emits would falsely "see no players this batch" and wrongly clear data the next batch was about to populate. **The right place is the parser** ([StormFrontParser.ts](src/main/parser/StormFrontParser.ts)). DR's XML protocol orders `<streamWindow>` (which carries the subtitle = room title) BEFORE any `<component id='room ...'/>` for the new room — deterministic, single-batch-or-not. New `lastRoomTitle: string` field on the parser; in the streamWindow handler, when `cleanTitle !== lastRoomTitle` emit `clear-stream` for all six room sub-streams BEFORE the typed `room-title` event. Components that DO arrive then re-populate via their own clear+stream-text flow; sections that DON'T arrive stay correctly empty. The `lastRoomTitle` gate prevents a no-op streamWindow re-emit (same subtitle, DR sends these for non-transition repaints) from clobbering freshly-populated sub-stream data. **B117 compatibility**: the parser's `captureSegments` work for monsterbold lives inside the `<component>` handler which fires AFTER the streamWindow pre-emit clear-streams — bold capture is unaffected. **Downstream beneficiary — contacts tracking**: the F34 encounter / `timeSpentMs` effects iterate `roomState.players`, so before B121 they were ingesting carryover ghost players from the previous room and crediting time-spent against the wrong room. B121's clear at the parser cascades to a clean `roomState.players: []` during the transition gap, so contacts stats are now accurate by construction (no separate fix needed for F34). **Don't move this logic back into GameWindow** — every fix at that layer has to know about batch boundaries, which makes it brittle. The parser sees the raw tag stream and knows the ordering. **Reset on session boundary**: `lastRoomTitle` is cleared in `parser.reset()` alongside other per-session state, so the first room of a new session always triggers clear-streams.

42. **Lichborne-source imports bypass the `ImportCandidate` intermediate; new rule types must update the bypass, not just the intermediate.** Established in B124 (v0.8.7). The import wizard's neutral intermediate at [types.ts](src/renderer/import/types.ts) (`ImportHighlight` / `ImportTrigger` / `ImportMacro` / `ImportAlias`) was designed to model the union of Wrayth / Genie / Frostbite source formats — it does NOT model the full native Lichborne rule shape. `ImportHighlight` has no `bold` / `glow` / `glowColor` / `groupIds` / `allGroups` / `name` / `enabled`; `ImportTrigger` has no `gates` / `oneShot` / `cooldownSeconds` / `watchStream` / nested action ordering / `groupIds` / `allGroups` / `name` / `enabled`; `ImportMacro` / `ImportAlias` are similarly trimmed. When F29 (Lichborne→Lichborne) shipped in v0.8.4, it routed everything through this intermediate, and the mapper at [mapper.ts](src/renderer/import/mapper.ts) hardcoded defaults for every field the intermediate didn't carry — silent data loss on round-trip across every rule type. **The fix shape**: `ImportResult` carries an optional `nativeRules?: { highlights, triggers, macros, aliases }` block (loose `unknown[]` typing to avoid a circular import with the rule modules). The lichborne.ts parser populates it in lockstep with the ImportCandidate arrays, spreading the source rule object and overriding `id: nanoid()` (and for triggers, every nested `TriggerAction.id` regenerates too). [ImportWizard.tsx](src/renderer/components/ImportWizard.tsx)'s `doImport` checks `result.nativeRules` and prefers it over `mapImportResult(...)` output when present; non-Lichborne sources leave it undefined and fall through to the legacy mapper path. **When adding a new rule type** (e.g. a future "scene", "snippet", "schedule"…), the new type needs: (a) full shape in the native rule module with `id` + `name` + `enabled` + `groupIds` + `allGroups` like every existing one; (b) an entry in `LichborneExportFile` and the parser's `nativeRules` block at [lichborne.ts](src/renderer/import/parsers/lichborne.ts) that spreads the source and regenerates ids; (c) an apply branch in `doImport` mirroring the existing highlights/triggers/macros/aliases shape — content-based dedup key, append-merge filter, save call. The ImportCandidate intermediate is **only** for surfacing the rule in the wizard preview UI; if you don't need preview support for the new type, you don't need an ImportCandidate at all (just `nativeRules`). The cardinal sin is the reverse — adding the field to ImportCandidate + mapper but forgetting to extend `LichborneExportFile`'s parser shape, which would re-introduce the B124 loss pattern. **Don't trust loaded rules to have default fields** — the runtime stores assume rules saved by Lichborne have all required fields, but a hand-edited YAML or a malformed export could be missing them. Defensive normalization belongs at the `loadHighlights`/`loadTriggers`/etc. layer, not the import boundary; we don't have it today because exports always come from `loadHighlights(character)` which returns runtime-complete shapes. Add it only when a tester actually hits a malformed-import crash.

43. **When the user can manually scroll past where the scroll API thinks the bottom is, the bug is in LAYOUT, not scroll math.** Established in B122 across v0.8.7 → v0.8.8. The half-row clip at font 13+ ate three failed hypotheses before landing on a real fix: (a) v0.8.7 lowered a `totalListHeightChanged` threshold from 2px → 0.5px — didn't help; (b) v0.8.8 first attempt replaced raw `el.scrollTop = el.scrollHeight - el.clientHeight` with `virtuosoRef.current?.scrollToIndex({ align: 'end' })` on the theory Virtuoso's internal accounting differed — didn't help; (c) Sekmeht's diagnostic — "I can manually scroll down ~half a row past where the snap lands" — proved BOTH APIs agreed on the wrong target. When two independent scroll APIs converge on the same position but the DOM has scrollable content past that position, the issue is in CSS / layout, not in the math. The fix that worked: a `components.Footer` on Virtuoso (`<div style={{ height: 14 }} />`, fixed 14px, gated to `settings.fontSize >= 13`) — defensive layout slack that doesn't require knowing why the scroll APIs land short. **General principle**: scroll-snap math problems are usually solvable by adding layout slack rather than by chasing what the math is doing. If you're tempted to write a third hypothesis-based fix to scroll math, stop — add invisible padding below the content instead. Specific to this case: the 14px footer is **gated on font ≥ 13** because at font 12 and below row heights round to clean integer pixels and no clip occurs; the slack would just be empty space below the last row (visible gap above the vitals strip). **Pair fix shipped in same release**: a separate `useEffect([settings.fontSize, settings.lineHeight, settings.largePrint])` re-snaps via `scrollToIndex({ align: 'end' })` ONLY when `pinnedRef.current` was already true — font / line-height / large-print changes reshape every row's height, growing scrollHeight, leaving the previously-pinned scrollTop short of the new bottom. Bail on `!pinnedRef.current` so scrolled-up readers aren't yanked back. rAF before the snap so Virtuoso has a frame to re-measure rows at the new dimensions. The threshold check in `totalListHeightChanged` is left at 0.5 (responsive) but doesn't carry the load — the footer does.

44. **The Room panel must mirror the main scroll's styling for the same content.** Established in B117 (v0.8.5, monsterbold creatures), reinforced in v0.8.8 (line-mode highlights for treasures). DR's `<component id='room players'/>` / `room objs` / `room creatures` / `room extra` sections are the exact same content that appears in the main scroll's "You also see ...", "Also here: ...", and (for desc) the room paragraph. Anything that styles that content in the main scroll — server-pushed bold/color, user contact name highlights, user match-scope highlights, user line-mode highlights — should style the structured Room panel sections the same way. Two things flow from this: (1) **the parser's `captureSegments` must carry the same styling fields as `pushSegment`** (currently: `bold` from B117; future styling sources will need to be added to both). When you add a new styling field to TextSegment (a `class`, `linkType`, whatever), update both the main-scroll `pushSegment` AND the capture path in `text()`'s `captureSegments.push`. (2) **RoomPanel's `renderSegments` must pass every kind of rule that TextLineRow passes** — `matchRules` (B111), `lineRules` (Rakkor v0.8.8), contacts, contact templates, nameRegex. The line-style is computed PER-SECTION (objects/players/creatures/extra each compute their own `getLineHighlightStyle` off their own joined text) so a rule that matches a player name paints only the Players section, not the others. `desc` deliberately stays out of line-style scope because it's multi-sentence prose where a single match would over-paint a large block. **B111's original "lineRules excluded on purpose" exclusion was overcautious and got overturned in v0.8.8** — the documented "lobster paints the whole section" objection turned out to be the wrong call: in the main scroll the same content gets that same paint when the user authored a line-mode rule, and consistency between surfaces beat the theoretical objection. Future "should the Room panel apply X?" questions should default to YES if the main scroll applies X for the same content.

45. **UI that should scale with `Settings → Font Size` must use `em` (inheriting from `.game-toolbar`) or `var(--game-font-size)` directly. Never `rem` for game-area UI.** Established in B113 (text weight pass), reinforced in v0.8.8 Mode button fix. `rem` is anchored to the browser root font-size (~16px) and ignores the user's per-character `settings.fontSize`. So a button declared as `font-size: 0.75rem` renders at a fixed 12px regardless of whether the user picked 8 or 24 in Settings. Three sizing categories in the codebase: **(a) toolbar buttons** like `.btn-debug` / `.btn-theme` / `.btn-settings` use `font-size: 1.0em` and inherit from `.game-toolbar` (which itself scales). New toolbar buttons should match this pattern — the Mode button shipped with `0.75rem` and visibly drifted smaller as users bumped their font size; v0.8.8 fixed it to `1.0em`. **(b) Portaled popovers** (Mode switcher menu, contact popover) can't inherit from `.game-toolbar` because they're mounted to `document.body` via React portal. Use `var(--game-font-size)` directly with a multiplier (e.g. `calc(var(--game-font-size) * 0.85)` for smaller sub-text). **(c) Text-line content** uses `var(--panel-font-size, var(--game-font-size))` to support the F31 per-panel override with a fallback to the global. When adding any new UI surface in the game area, grep for `rem` in the new CSS before committing — every `rem` is a fixed pixel size that won't track user preference. App chrome (settings panels, modal headers, etc.) can use `rem` deliberately if it should NOT scale with game font, but that's the exception, not the default.

46. **DR's room-transition data arrives in TWO independent XML tags; the parser must read both, not just one.** Established in B126 (v0.8.8). When the player moves to a new room, DR usually emits both: (a) `<streamWindow id='main' subtitle='[Title - rmId]'/>` — carries the room title and room id together inside the subtitle bracket, parsed in [StormFrontParser.ts:441-484](src/main/parser/StormFrontParser.ts#L441-L484), and (b) `<nav rm='X'/>` — carries the new room id as an attribute, parsed in [StormFrontParser.ts:523-548](src/main/parser/StormFrontParser.ts#L523-L548). For most transitions both arrive together so reading just `<streamWindow>` is sufficient. **But some transition shapes (teleports, NPC-induced moves, certain scripted moves) emit ONLY `<nav rm>` and skip `<streamWindow>` entirely.** For those, a front-end that reads only `<streamWindow>` will have `roomState.title` and `roomState.roomId` frozen on the prior room until a manual `LOOK` forces a re-emit. Lich's `$room` variable is correct the whole time because Lich's scripting reads `attrs.rm` from `<nav>` independently — that's the canary: when `;e $room` shows the right room but the Lichborne map shows the wrong one, we missed a `<nav rm>` somewhere. **The fix shape**: a separate `RoomIdEvent` (not an overloaded `RoomTitleEvent`) — emit `{ type: 'room-id', roomId: N }` from the `<nav>` handler when `attrs.rm` is numeric. The handler in GameWindow routes it into `roomUpdates.roomId` ONLY — leaves title / desc / sub-streams alone, because forging an empty string for fields we don't have would wipe the only data we have (the prior room's title is at least *something* to display until a real `<streamWindow>` arrives). **Don't overload RoomTitleEvent with an empty title for the `<nav>` case** — `GameWindow`'s `case 'room-title'` does `roomUpdates.title = evt.title` unconditionally; an empty title would clear the prior title and the Room panel would go blank. Two events with different invariants is the right separation. **Scope of the Lich Map fix**: it's not a fix for the Room panel's title-display (only `<streamWindow>` carries the title string), and not a fix for Genie Map (its `currentLocation` memo is title-based, not roomId-based; B110's `staleZoneCountRef` escape valve still handles Genie's variant of the symptom). **General rule**: when extracting data from a stateful XML stream, audit every place the same data could appear. Room id is in two places (streamWindow subtitle + nav rm); both need handlers. The same audit should apply for any future "data appears in multiple tags" case.

47. **Save calls on multi-category data sources must be gated per-type on `incoming.length > 0`.** Established in B127 (v0.8.9). The import wizard's `doImport` writes to four independent per-character stores: highlights, triggers, macros, aliases. Plus four F29-native stores: groups, modes, contacts, contactTemplates. Different source types fill different subsets of these — Wrayth's parser returns `[]` for triggers and aliases (Wrayth's XML doesn't carry them); Frostbite returns `[]` for triggers, aliases, and contacts; Genie returns all five; F29 returns whatever the source character had. Pre-v0.8.9, BOTH the Append and Replace branches in [ImportWizard.tsx](src/renderer/components/ImportWizard.tsx) `doImport` called `saveX(character, mapped.X)` (or `[...existing, ...keepX]` in Append) for ALL four rule types regardless of whether `mapped.X` was empty. The catastrophic failure mode was Replace: `saveTriggers(character, [])` wipes the user's existing triggers when the current import has no triggers. Append was mathematically a no-op for empty arrays but indistinguishable in correctness from a future regression — the right shape is to gate the save itself, not rely on save-array math being benign. **The fix shape**: every save call (rules AND F29 native data) gated on `mapped[type].length > 0` (or `result.nativeX.length > 0` for native data — the outer `result.nativeX && Array.isArray(...)` check passes for empty arrays too because `[]` is truthy and `Array.isArray([])` is `true`, so a Lichborne F29 export from a character with no groups would have wiped the recipient's groups on Replace pre-fix). **When adding a new rule type** to the import system (e.g. a future "scene", "snippet", "schedule"…), follow the same pattern: gate every `saveX(...)` call on the corresponding `mapped[type].length > 0`. The Append-branch dedup math (`[...existing, ...keepX]`) is still correct for non-empty incoming and should run normally. **When adding a new parser** that doesn't carry some rule types (like Wrayth doesn't carry triggers), explicitly return `[]` arrays for the missing types — don't omit the field from the result object, because every consumer assumes the field exists. The per-type guards mean the empty array just no-ops the save; no special handling needed at the parser level.

48. **Genie's complete `#command` catalog lives at [Genie4 source Command.cs:270](C:\temp\Genie4-Dev\Genie4\Core\Command.cs#L270) (`switchExpr = oArgs[0].Substring(1).ToLower()` then ~100 case branches). When adding a new import-side recognizer, check the full catalog first to identify aliases — Genie has multiple `#commands` for the same operation.** Established in B133 (v0.8.9). The clean mappings to Lichborne action types are deliberately narrow because most Genie commands have no front-end equivalent: **(a) Game commands** — `#send`, `#put`, `#q`, `#que`, `#queue` → `command` action; **(b) Variables** — `#var`, `#variable`, `#setvar`, `#setvariable`, `#tvar`, `#tempvar`, `#tempvariable`, `#svar` → `variable` action (Lichborne doesn't distinguish var scope at the action layer — temp/saved/string/server vars all collapse to the same action); **(c) Sounds** — `#play`, `#playsound`, `#playwave` → `sound` action; **(d) Beep** — `#beep`, `#bell` → `beep` action; **(e) Flash** — `#flash` → `flash` action; **(f) Echo** — `#echo` → `echo` action; **(g) Log** — `#log` → `log` action. **Everything else gets dropped with a "Unsupported actions skipped: #X, #Y" note in the import preview** via the catch-all `else { dropped.push(part) }` at the end of `parseActionParts` in [genie.ts](src/renderer/import/parsers/genie.ts). The non-mappable categories: **rule manipulation** (`#trigger`, `#untrigger`, `#alias`, `#unalias`, `#macro`, `#unmacro`, `#highlight`, `#unhighlight`, `#class`, `#unclass`, `#name`, `#unname`, `#gag`, `#ungag`, `#sub`, `#unsub`, `#substitute`, `#unsubstitute`, `#ignore`, `#unignore`, `#squelch`, `#unsquelch`, `#unvar`, `#unvariable`, `#unsetvar`, `#unsetvariable` — modify rules at runtime, which Lichborne's static rule model doesn't support); **UI/window control** (`#statusbar`, `#window`, `#position`, `#layout`, `#show`, `#hide`, `#close`, `#edit` — Genie-specific UI); **connection control** (`#connect`, `#disconnect`, `#exit`, `#reconnect`, `#lc`, `#lconnect`, `#lichconnect`, `#ls`, `#lichsettings` — would let a trigger forcibly disconnect; user should do this from the UI); **system/config control** (`#plugin`, `#load`, `#save`, `#reload`, `#unload`, `#config`, `#set`, `#setting`, `#settings`, `#profile`); **movement/mapping** (`#mapper`, `#map`, `#walk`, `#walkto`, `#go`, `#goto`, `#path` — could partially map to Lich `;go2` but routing differs); **scripting flow** (`#if`, `#parse`, `#regex`, `#regexp`, `#do`, `#goto`, `#true`, `#false`, `#wait`, `#waitfor`, `#pause`, `#abort`, `#resume`); **math/random/eval** (`#math`, `#evalmath`, `#random`, `#eval` — Lichborne variable actions only store, don't compute); **custom Genie events** (`#event`, `#events`). **`##escape` syntax** — Genie uses `##text` to send a literal `#text` to the game (escape for the `#` prefix); the parser strips the leading `#` and treats as a plain command. **Explicit no-ops** — `#nop` and `#comment` are silently dropped without "Unsupported" warning since the user never intended them to do anything; flagging them would scare users with a useless note. **`#send #X` / `#put #X` recursive shape** — see pitfall #X (TODO if separate, otherwise documented inline in [genie.ts](src/renderer/import/parsers/genie.ts) at the COMMAND_PREFIX_RE branch). When the inner content of a send/put wrapper itself starts with `#`, recursively process via `parseActionParts` instead of treating as a DR command — this handles user-error shapes like `#send #flash` (Jaded's B130 case).

49. **`STREAM_FALLBACK` and DR's native protocol duplication can double-count.** Established in B136 (v0.8.10). DR's XML protocol sends some streams TWICE per server event — once inside a `<pushStream id="X"/>` block (which routes to the X panel), and once OUTSIDE the block to main directly. **Speech and whispers do this; thoughts / arrivals / deaths / etc. do not.** A `say hi` produces literally: `<pushStream id="talk"/><preset id='speech'>You say</preset>, "Hi."<popStream/><preset id='speech'>You say</preset>, "Hi."` — two complete copies, the second going to whichever stream is active outside the pushStream block (typically `main`). Lichborne's [`STREAM_FALLBACK`](src/renderer/components/GameWindow.tsx) routes unwatched stream content to a fallback (typically `main`) so important text doesn't get silently buffered. **Adding a STREAM_FALLBACK entry for a stream that DR already natively duplicates to main produces double-counting** — the inside-pushStream copy gets fallback-routed to main on top of DR's outside-pushStream copy. Symptom: "You say, Hi." appears twice in main when no panel watches that stream. **Rule**: when adding a new stream to STREAM_FALLBACK, FIRST check whether DR natively emits an outside-pushStream copy for that stream. If yes (speech / whispers — and any future stream that follows that pattern), DO NOT add a fallback entry. If no (thoughts, arrivals, deaths, custom Lich script streams that emit only inside the pushStream block), add the fallback. The B136 fix removed `conversation: 'main'` from STREAM_FALLBACK for exactly this reason; the comment block at the STREAM_FALLBACK declaration documents the principle inline. **How to check**: open Debug → Raw XML, do a `say test`, look for two consecutive copies of the speech in the XML capture — one inside `<pushStream id="talk"/>...<popStream/>`, one immediately after. If you see that pattern, the stream is natively duplicated and shouldn't have a fallback.

50. **Stream id aliases live in [`src/shared/streamAliases.ts`](src/shared/streamAliases.ts); use `normalizeStreamId(id)` everywhere a stream id crosses a boundary.** Established in B135 (v0.8.10). Pre-v0.8.10, the parser had its own inline `STREAM_MAP` constant for translating server / legacy / sibling-client stream names to Lichborne panel ids (e.g. `talk` → `conversation`, `death` → `deaths`). When we needed the same translation in the renderer's `echoToStream` (so imported Genie triggers with `#echo >talk` would land in the right panel) and in the import parsers (so legacy plural references on imported triggers got rewritten), the choices were either (a) duplicate the map in each module — error-prone drift risk — or (b) extract to shared. The shared module is `STREAM_ID_ALIASES` (the bare map) + `normalizeStreamId(id)` (helper that returns id unchanged if not aliased). Used at: parser's `<pushstream>` / `<streamwindow>` handlers, parser's `<clearstream>` handler (uses `STREAM_ID_ALIASES[id]` directly to preserve COMPONENT_STREAM fallback chain), renderer's `echoToStream` in GameWindow, Genie parser's `parseEchoAction` (normalizes `>stream` tokens at import time), F29 `lichborne.ts` parser (normalizes legacy `watchStream` / `echoStream` on imported triggers). **When adding a new alias** (e.g. a future Lich script emits a new variant id you want to merge into an existing panel), add ONE entry to `STREAM_ID_ALIASES`. All five call sites pick it up automatically. **When adding a new builtin panel type**: add the canonical id to [`PanelFrame.tsx`](src/renderer/components/PanelFrame.tsx) `PanelType` / `PANEL_LABELS` / `ALL_PANEL_TYPES` AND add a switch case for it. If multiple ids should route to it, add aliases too. **When renaming an existing panel id** (B135's `conversations` → `conversation` precedent): write a localStorage migration in [`src/renderer/localStorageMigrations.ts`](src/renderer/localStorageMigrations.ts) that walks per-character tabs / activeIds / panelFontSizes keys / trigger watchStream values. **Case-insensitive matching** in the migration is important — user data may include custom-typed tabs created from a discovered stream id with arbitrary casing (e.g. `'Conversations'` if Lich emitted `<streamWindow id='Conversations'/>` at some point). **Dedupe pass after rename** is also important — if a user had both the builtin tab AND a custom variant, both collapse to the new id and the duplicate should be dropped, not left as two visible duplicate panels. **Migration flag bumping** (e.g. `conversationRename` → `conversationRename2` → `conversationRename3`) is the recovery mechanism if a draft migration ships with a bug — bumping the flag re-runs the (now-fixed) migration on every user's next launch. **When writing a rename migration, walk NESTED fields too — not just top-level**. The v0.8.10 retroactive sweep caught a real example: the trigger handler in `localStorageMigrations.ts` originally only renamed `t.watchStream` but a `TriggerRule` also has `t.actions[]` with `echoStream` on echo actions. A pre-v0.8.10 user with a trigger that echoed to the legacy panel id had stale `echoStream` left after the migration — functionally OK via runtime alias normalization but the trigger editor showed the legacy value outside its dropdown options. Fix: extend the handler to walk `actions[].echoStream` too, bump flag to `conversationRename3`. **General rule**: when a rename touches a domain object (Contact / Trigger / Macro / etc.), enumerate ALL fields where the renamed string could appear — including nested arrays — and rename in all of them. The runtime alias map in `STREAM_ID_ALIASES` is a safety net, NOT a substitute for clean data; users editing rules through the UI need the stored value to match the displayed options.

51. **Macro `@` is the universal cursor-position marker; `\@` escapes to literal `@`.** Established in B137 (v0.8.10). Genie ([`FormMain.cs:1140`](C:\temp\Genie4-Dev\Genie4\Forms\FormMain.cs#L1140) `ParseInputBox` + [`FormMain.cs:4079`](C:\temp\Genie4-Dev\Genie4\Forms\FormMain.cs#L4079) `ClassCommand_SendText`) and Wrayth both use `@` as the cursor-position marker in macro commands. The presence of an unescaped `@` switches the macro from "send immediately" to "type into the input box and wait for the user to finish typing" mode. **Lichborne's canonical helper** is [`parseCursorMarker(command)`](src/renderer/macros.ts) in `macros.ts` — walks the string char by char, treats `\@` as a literal `@` in the output (escape), strips all unescaped `@` chars from the typed text, returns `{ text, cursorPos }` for the first unescaped `@`'s position. Returns `null` when no cursor marker is present (caller sends normally). **Runtime in the macro fire path** ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)): for each command, check cursor marker FIRST (before token check). If found: flush any pending send-mode commands, `setCommand(text)` + sync `commandRef.current`, wait one rAF for React to commit the input value, then `inputRef.focus()` + `setSelectionRange(cursorPos, cursorPos)`. Stop iterating remaining macro commands (the user must hit Enter to send what's in the bar; anything after the cursor command would never fire). **Wrayth `\r` translation at import time** ([wrayth.ts](src/renderer/import/parsers/wrayth.ts) `parseWraythAction`): Wrayth's `\r` is the explicit "send / Enter" marker; absence of `\r` means "type and wait." To map Wrayth's signal into Lichborne's universal `@` convention, the importer tracks whether the raw action ended with `\r` — if NOT, and the last command doesn't already have an unescaped `@`, append `@` so the runtime treats it as type-and-wait. Jaded's `<k action="&apos;}"/>` (decoded `'}`, no `\r`, no `@`) imports as `'}@` and works correctly. **Genie imports**: Genie macros use `@` natively, but Genie's file format ALSO uses Level-1 escape sequences (`\\` → `\`, `\}` → `}`, `\{` → `{`) for content that contains the argument-delimiter characters. Pre-v0.8.10 our `parseArgs` used `line.indexOf('}')` and terminated arguments at the first `}` even when escaped — a Genie compound macro like `{\\x;'\}sekmeht @}` parsed as `\\x;'\` (the `\}` was treated as the terminator) and the rest got dropped. v0.8.10 rewrites parseArgs to walk char-by-char and resolve Level-1 escapes during extraction. **`\x` handling — deliberate simplification (Sekmeht 2026-06-01)**: Genie's runtime has a two-mode model where `\x` triggers "clear + type" and `@`-without-`\x` triggers "insert at cursor in current input." Lichborne picks ONE mode — "always clear and type when there's an `@`" — because (a) it's simpler and more predictable for testers and (b) every real-world `\x` macro from Jaded's file also has `@`, so the simplification covers the common case. So in Lichborne: **`\x` is purely import-time noise** — `splitAction` strips it from each part and lets empty parts get filtered out. **Only `@` triggers wait-mode**, not `\x` alone. `\\x;hide` (hypothetical) imports as just `hide` and auto-sends; `\\x;whisper group @` imports as `whisper group @` and waits (because of `@`). The "insert at cursor without clear" mode isn't supported — if a tester ever genuinely needs it, we'd add a runtime flag (e.g. a leading `\x` in the stored Lichborne command means "insert at cursor instead of replacing"). **Genie variables** (`$speak`, `$whisper`, `$emote`, etc.) are NOT expanded by Lichborne — they pass through `interpolate` (which only handles names that match Lichborne's reserved variable list) and stay as literal `$varname` text in the imported command. Future feature. **Multi-command behavior**: if a macro has both send-mode and wait-mode commands, the send-modes fire first, then the wait-mode command deposits text and stops; commands AFTER the wait-mode command are silently skipped (matches Genie's behavior — once the cursor is in the input box, the macro is "done" until the user hits Enter, which sends only the box contents). **Backward compat for manually-created Lichborne macros** with literal `@` in the command text (uncommon — usually for DR's `SAY @person message` syntax, but people typically type that directly): post-v0.8.10 the `@` is a cursor marker; users with such macros need to change `@` to `\@` to preserve literal `@` send-mode. **When adding a new macro feature** that interacts with command text (variable interpolation, command chaining, etc.), be aware that `parseCursorMarker` runs AFTER interpolation (`resolveMacro` interpolates `$varname` before returning) — so a variable that expands to text containing `@` would trigger cursor-marker handling. Could be a feature (variable-driven cursor positioning) or a bug depending on intent.

52. **`;vars setup` (and Lich GTK setup-window scripts in general) crash Lich on user interaction; route testers to Lichborne's SQLite vars viewer instead.** Investigated 2026-06-01 (v0.8.11). The `;vars setup` script lives at `Ruby4Lich5/Lich5/scripts/vars.lic` (it's a core Lich script, not a dr-script — `dr-scripts/` has no `vars.lic`, only `setupaliases.lic`). It opens a GTK window via `Gtk.queue { Gtk::Window.new ... window.show_all }` and blocks on `wait_until { done }` until the `delete_event` handler fires. The window DOES paint under Lichborne (which contradicts the older "kill-counter shape" assumption that all such windows fail to render — see updated architecture note above). **What crashes Lich:** [`vars.lic:136-141`](C:\Ruby4Lich5\Lich5\scripts\vars.lic#L136-L141) spawns a `Thread.new { ... sw.vadjustment.value = ... }` block that mutates a GTK widget (`sw.vadjustment`) from a non-main thread inside the "focus-in" event handler that fires when the user clicks the "(new var name)" textbox. This directly violates the warning at [`gtk.rb:4`](C:\temp\lich-dev\lich-5\lib\common\gtk.rb#L4) — "Calling Gtk API in a thread other than the main thread may cause random segfaults." Under Lichborne's launch (`ruby lich.rbw --stormfront --dragonrealms`) the race appears to manifest reliably; under Genie's launch it apparently doesn't (GTK runtime timing differs between front-end environments — both run the same `vars.lic`, so the difference is in how the GTK loop is hosted). The failure shape: user focuses new-var textbox → `Thread.new` launches and mutates the scrolled-window adjustment from off-thread → GTK widget state race → Ruby/GTK segfault → Lich process exits → Lichborne's [LichConnection](src/main/connection/LichConnection.ts) sees the socket close and emits `disconnect`. **This is a Lich-script bug, not a Lichborne bug.** It cannot be fixed from Lichborne's side without forking Lich's `scripts/vars.lic`, which is out of scope (Principle: not a Lich-replacement; we don't ship script forks). **Lichborne-native replacement SHIPPED in v0.9.0** — the Lich Dashboard → Variables tab is now a full add / edit / delete editor (see pitfall #53). It deliberately did NOT take the approach this pitfall originally speculated (a Marshal *serializer* + direct `lich.db3` write): a direct DB write is unsafe while Lich is running because Lich's in-memory `@@vars` is authoritative and its next auto-save would clobber it (vars.rb). Instead writes go THROUGH Lich's public `Vars` API via `;eq Vars['name'] = value; Vars.save`, which mutates Lich's memory AND forces an immediate flush — no Marshal serializer needed, no race with Lich's auto-save. Read still uses the SQLite/Marshal path (`lich:get-vars`). **When a tester reports the `;vars setup` disconnect:** confirm root cause matches (window opens fine, disconnects only on adding/editing), explain it's a Lich-script GTK race that manifests under the `--stormfront` launch shape, and route them to the existing vars viewer for read access plus the script's NON-GTK sub-commands `;vars set NAME=VALUE` / `;vars delete NAME` / `;vars list` (vars.lic:41-75 — these don't touch GTK at all and work fine). **Related discovery, correcting CLAUDE.md drift:** the older "Known limitation — Ruby GTK script windows don't render" architecture note claimed "force-mode (`--stormfront`) skips Lich's entry GUI, which is what starts the GTK main loop" — that's factually wrong. [`lich.rbw:135`](C:\temp\lich-dev\lich-5\lich.rbw#L135) calls `Gtk.main` UNCONDITIONALLY when `defined?(Gtk)` (i.e., whenever the `gtk3` gem loads), and [`gtk.rb:148-155`](C:\temp\lich-dev\lich-5\lib\common\gtk.rb#L148-L155) registers a `GLib::Idle.add { sleep 0.01 }` yield so Ruby's I/O threads can run while the GTK loop spins. So the loop IS running under `--stormfront`. The kill-counter family does NOT "never appear" either — Sekmeht confirmed (v0.9.0 testing) that `kill-counter.lic`'s window surfaces **intermittently** (often after a start→kill→restart cycle), so the shared root cause is GTK-runtime timing/threading flakiness under `--stormfront`, not a missing pump and not a categorical no-paint. A painted kill-counter window is still unsafe — interacting with it can crash Lich the same way `;vars setup` does. Don't repeat either the "no main loop" or the "windows never appear" claim without re-verifying. The architecture-section paragraph above has been corrected in the same v0.8.11 update. **General principle for future Lich-GTK-script disconnect reports:** read the actual `.lic` source first (the user's live install at `Ruby4Lich5/Lich5/scripts/` is the canonical location; `dr-scripts/` does NOT contain core scripts like vars/log/repository), look for `Thread.new` blocks that touch GTK widgets from off-thread, and check `gtk.rb` for the thread-safety contract before blaming the front-end launch shape. The disconnect signature in Lichborne is the same regardless of cause (Lich exits → socket closes → `LichConnection` emits `disconnect`); the diagnostic value is in the .lic source, not the front-end.

53. **Editing Lich state writes THROUGH Lich's runtime API, never into `lich.db3` directly — and force a save so the read view doesn't lag.** Established by the v0.9.0 editable-variables feature in [LichDashboard.tsx](src/renderer/components/LichDashboard.tsx) `VarsTab`. The shape generalizes to any future "edit a thing Lich owns" feature (vars, settings, the repository list, etc.). **Why not write the DB:** while Lich is connected, its in-memory copy is authoritative (for vars, the `@@vars` hash in [vars.rb](C:\temp\lich-dev\lich-5\lib\common\vars.rb)); Lich persists to `lich.db3` only on its own schedule (vars: a 5-min auto-save, and only when the md5 changed) and rewrites the WHOLE blob via `INSERT OR REPLACE`. A direct Lichborne DB write would be silently clobbered the next time any Lich script touches a var. So Lich's memory is truth and the DB is just its persistence — same inversion as Principle #1's "YAML is truth, localStorage is a working copy," but here Lich owns the truth. **The write path:** send a single atomic `;eq <ruby>` (ExecScript — `;e`/`;eq` is dispatched at [global_defs.rb:2319](C:\temp\lich-dev\lich-5\lib\global_defs.rb#L2319), `;eq` = the quiet form) that BOTH mutates via Lich's public API AND forces the flush in one statement list: `;eq Vars['name'] = value; Vars.save` (delete = `... = nil; Vars.save`). One `;eq` (not two separate commands) because two commands race — a standalone `Vars.save` could run before a separate `;vars delete` finished, persisting the pre-delete state and leaving the DB stale until the 5-min timer. `Vars.save` is mutex-guarded (vars.rb:78) and rewrites the whole blob, so a deleted key actually leaves storage and the SQLite read view is correct on the next refresh. `Vars[]=`/`Vars.save` are Lich's documented public API (as stable as the `;vars` command, and what `;vars` calls internally) — this is NOT "running arbitrary Ruby," it's the same API surface. **Escaping:** values/names are injected into Ruby single-quoted literals via `rubyLit(s)` (escape `\` and `'` only — those are the only specials in a Ruby `'...'` string) so values with quotes/spaces/backslashes survive. Mirror Lich's own coercions (vars.lic coerces literal `true`/`false` to booleans — so do we, for display parity). **Silent send:** the `;eq` runs via a no-echo path (`onRunCommand` wired to `window.api.sendCommand(sessionId, cmd)` directly, NOT the echoing `sendCommand`) so the raw Ruby doesn't spam the game window — the optimistic UI update is the user-facing feedback. **Read stays SQLite** (`lich:get-vars` + Marshal) — structured display of lists/hashes/times, cross-scope browse, graceful per-row parse-error degradation. The read/write asymmetry is deliberate and correct: read wants structure + any-scope browsing (the Marshal storage format is a far more stable contract than the cosmetic `;vars list` output, and command-read would need a stream-capture/suppress mechanism like the `;listall` poll); write must mutate live memory. **Two gates on edit controls**: (a) `session.useLich` AND (b) the selected scope equals the connected character's own scope (`${game}:${character}`) — `;eq Vars[...]` only touches the CONNECTED session's memory, so editing while viewing another character's scope would silently write the wrong character's vars. Viewing other scopes is read-only with a note. **Optimistic overlay** (`pendingRef`, keyed by scope → name → change) bridges the sub-second gap between the write and the next DB read so ↺ / reopen never flicker an edit back to its old value; it survives a refresh by re-applying over the DB read, and is discarded on modal close (the forced save makes the DB authoritative-fresh by then, so this is belt-and-suspenders). **When adding a new "edit Lich state" surface**: follow this exact shape — write through Lich's public Ruby API + force its save in one `;eq`, read through whatever structured path exists, gate on connected-own-scope, send silently, overlay optimistically. Don't reach for a direct DB writer or a Marshal serializer; the race with Lich's in-memory authority is the trap.

54. **(Removed v0.9.x — number kept as a tombstone.)** This pitfall documented the v0.9.0 GTK-script advisory: a `client-notice` IPC channel that injected `--- Lichborne:` lines into the stream, plus a `maybeWarnGtkScript` / `GTK_CODE_RE` / `resolveLichScriptFile` detector in [main.ts](src/main/main.ts) that scanned a started script's `.lic` for GTK code and warned the user. **All of it was removed** when the GTK-friendly launch shape shipped (see the "Ruby GTK script windows" architecture note + the launch section) — the advisory assumed GTK was broken, which is no longer the operating assumption. The `client-notice` channel (its only consumer was this advisory), the `onClientNotice` preload/`global.d.ts` binding, the GameWindow subscription, and the per-session `gtkChecked`/`useLich`/`lichPath` Session fields are all gone. **Transferable lesson worth keeping** (this is why the number isn't just deleted): *any* future feature that reacts to "the user started script X" — a per-script setting, a script-specific panel, a fresh advisory — must resolve the script the way Lich's `Script.start` does ([script.rb:65-84](C:\temp\lich-dev\lich-5\lib\common\script.rb#L82)): build the candidate list (custom/ root + subdirs, then scripts/ root, each sorted by basename) and match **exact first, then prefix** — because users abbreviate script names constantly (`;kill-cou` starts `kill-counter.lic`). Don't assume `;typed` maps to `typed.lic`. **And if you ever need to inject a client-authored line into the stream again** (the old `client-notice` pattern): a segment with `bold: true` and NO `preset` renders as monsterbold yellow, because `renderSegment.tsx` resolves `preset = seg.preset ?? (seg.bold ? 'bold' : undefined)` — an explicit preset (e.g. `internal-system`) WINS over the bold fallback and gives you a dim gray-bold line instead. Rebuild the channel from scratch if needed rather than expecting the old wiring to still exist.

---

## Proactive bug-checking

The goal is to find bugs before testers do — both new bugs the current change might introduce, and **latent bugs from prior versions that are still living in the current code**. Lichborne is single-developer with a small tester pool; every bug we ship is one a tester has to find and report, costing them and us time. Two complementary practices.

### Iron rule: read the actual code, not memory or docs

Every claim you make about how the code behaves must come from reading the file you're claiming about, IN THIS SESSION. Not from CLAUDE.md, not from pitfalls, not from a Tracker.md entry describing the code, not from "I remember how this works." Documentation describes the code's intent and history; only the source describes what it currently does. The B114 false-positive that prompted this rule (v0.8.7 retroactive sweep): pitfall #33 described the theme-overlay re-apply hook pattern, I worked from that description, claimed a multi-character bug existed, and was wrong — the actual effect at [GameWindow.tsx:954-973](src/renderer/components/GameWindow.tsx#L954-L973) had `isActive` in its deps and re-applied overlays directly on tab switch, which the pitfall didn't mention because the pitfall was scoped to a different scenario. Acting on the false positive would have shipped a redundant fix and possibly introduced a real new bug. The fix-cost of a false positive is higher than the fix-cost of doing the read.

**Verification protocol** when claiming a bug exists or doesn't exist:
- Grep for the specific function / state / effect you're reasoning about.
- Read the relevant lines yourself (Read tool with line offsets, not just the grep snippet — grep context can mislead by hiding deps arrays, guard clauses, or cleanup returns).
- Confirm the deps array of any `useEffect` you reason about — pitfalls describe patterns, not necessarily the current deps list.
- Confirm the cleanup function — pitfalls describe what's supposed to happen, not necessarily what does.
- If a pitfall and the current code disagree, **the code wins** — and the pitfall needs updating.

This rule has no "n/a." Every retroactive sweep claim, every "this is safe because…" assertion, every "no concerns" sign-off must be backed by a read in this session. The cost is 30 seconds of grep + read per claim; the benefit is not shipping false-positive fixes (and not missing real bugs that contradict your memory of how something works).

### Pre-merge checks

Before declaring a change done, run through these three questions. ~30 seconds per change, catches the classes of bug this codebase has actually shipped (B114 cross-tab overlay drift, B121 cross-feature state writes, the kind of stale-state-vs-new-code mismatches that come from incremental migrations). The right answer to any of these can be "n/a" — the point is to have considered it.

1. **What state does this write that other code reads?** List every var / ref / state your change writes. For each, grep one consumer and read it. Cross-feature state is where the bug class lives: B121 cleared `roomState.desc` and three other subsystems (Room panel, Lich Map findRoom, Genie Map memo, contacts effect) read it. The pre-check found the same-batch behavior was already absorbed by existing protections (B109 timer, F34 early-return); the after-the-fact discovery would have shipped as B12X. If your change writes nothing other code reads, the answer is "n/a" — say so.

2. **What cleanup runs on unmount vs. on hide?** Tabs in Lichborne don't unmount when you switch — they go `display:none` (pitfall #24). So `useEffect` cleanup only fires on real unmount (close session), not on hide. Anything that should re-fire on *show* (re-apply per-character settings, refresh layout-dependent measurements, reset transient state) needs an explicit activeId-watcher in App.tsx, NOT just a mount-effect in GameWindow. B114 shipped with this gap: theme-overlay re-apply only ran on theme change, not on tab switch, so two characters with different accessibility overlays bleed into each other. If your change relies on a mount/unmount lifecycle, ask "does this need to fire on show/hide too?"

3. **Does the current code handle state written by older versions of itself?** YAML profile files, localStorage entries, the genie parse cache, the panel layout state, custom themes — all of these were written by some prior Lichborne version a user already ran. When your change reads any of this data, what happens if the field is missing (older write), has a different shape (intermediate refactor), or has a value the new code doesn't expect? **Backward compatibility we care about runs in one direction only**: current code reading prior-version state. We do NOT care about older clients reading newer files — testers run the current build, not v0.8.4. The right answers: (a) treat missing fields as their type default (most optional YAML), (b) bump GENIE_CACHE_VERSION when the cached shape changes (pitfall #16), (c) write an explicit migration in `profile-migrations.ts` by SOURCE version when a field is renamed / restructured. The wrong answer is reading stale state and crashing / silently dropping data the user previously saved.

These are not a substitute for testing — they're a substitute for the bug report you'd otherwise get next week. The right output is either "no concerns" + a one-line note in the commit message, or a tracked issue if a concern surfaces that's out of scope for the current change.

### Retroactive sweeps at version boundaries

When cutting a new version (or whenever there's a natural pause), do a focused read of the last 3–5 versions' Tracker entries with a different question: **not "did the change work" but "what could a tester still hit that hasn't been reported yet?"** The pattern that ships dormant bugs in this codebase:

- A new feature (Fxx) writes state or uses an effect lifecycle in a way that's correct for its own scope but interacts unexpectedly with an OLDER subsystem the author wasn't focused on.
- The bug is silent — no exception, no obvious wrong rendering. Just a corner case where state drifts (B114), focus jumps unexpectedly (F32 edge cases), or stats accumulate against the wrong target.
- It takes a specific user configuration to trigger: multi-character + accessibility overlay (B114), Ctrl+Tab from a modal text input (F32), Lich script with both `<streamWindow>` and `<pushStream>` for the same id (B123).

For each recent feature, ask the three pre-merge questions retroactively. Read the consumers of any state it writes. Verify the cleanup story on show/hide. Check whether older saved state still loads correctly. Don't refactor on speculation — log a real concern with a repro path, or call it clean. The retroactive sweep done for v0.8.7 found B114 (real, fixed in v0.8.7) and noted F32 edge cases (deferred, not fix-worthy without a tester report). That's the right output shape: distinguish "real bug worth fixing" from "future-feature concern" from "minor papercut without a clean fix" and don't conflate them.

**When to do this**: at the cut of every minor version (v0.x.0 → v0.x+1.0) at minimum, and ideally before each patch release when a meaningful feature shipped recently. The cost is one focused review session; the alternative is a tester finding it cold and the trust hit that comes with shipping obvious-in-retrospect bugs.

---

## Commands

```
npm install              # one-time
npm start                # build main + renderer, launch Electron
npm run build            # build only, no launch
npm run build:main       # esbuild → dist/main/main.js, preload.js
npm run build:renderer   # vite → dist/renderer
npm run dev:renderer     # vite dev server on :5173 (NODE_ENV=development)
npm run dist             # build + electron-builder NSIS installer → release/
```

No unit tests — verify changes by running `npm start` and exercising the UI.

Releases ship via `node publish.mjs` (needs `$env:GH_TOKEN`). Cleans `release/`, builds, packages, uploads draft to GitHub, patches release body from `release-notes.md`. **Do NOT create git tags manually** — electron-builder creates `vX.Y.Z` during publish.

---

## Documentation discipline

**"Update documentation"** means: **BUGS.md, DESIGN.md, Tracker.md, README.md, CLAUDE.md** (5 files; CLAUDE.md added to the list 2026-05-17).

| File | Role | When to update |
|---|---|---|
| **CLAUDE.md** (this) | AI-facing guide. Behavior, conventions, pitfalls. | When a subsystem's *shape* changes, when a new pattern emerges, when a pitfall is discovered. Keep tight. |
| **DESIGN.md** | Technical spec, source of truth for *how* things work. | *Before* implementing significant UI / architecture changes. It's a spec, not a journal. |
| **Tracker.md** | Version history. Brief why/how per release. | When a release ships. Reverse-chronological. |
| **README.md** | User-facing. | When behavior visible to users changes. |
| **BUGS.md** | Tester-reported bug ledger (B-numbers). | Open / Resolved tables, with full root-cause notes on resolution. |
| **release-notes.md** | GitHub release body. User-readable changelog. | Separate from Tracker.md (dev-facing). **Keep only the last 1–2 releases** — prepend the new version's section, then trim so the file holds just the current release plus (optionally) the immediately prior one for context. Do NOT keep a running trail of every release here; the full version history lives in Tracker.md and git. When cutting a new version, add the new section at the top and delete everything older than the prior release. |

---

## Operational guardrails

- **Never `git commit` without explicit user approval.** Asking is mandatory.
- **Windows + PowerShell** is the host shell. Use PowerShell syntax (`$null`, `$env:VAR`). Bash available for POSIX scripts.
- `profiles/` is gitignored; never commit user data captured during testing.
- `passwords.json` lives in `app.getPath('userData')`, encrypted with Electron `safeStorage` (Windows DPAPI). Don't move it into the repo tree.
- Prefer fixing root causes over adding fallbacks.
- **When proposing a feature, check the principles first.** If it duplicates Lich functionality, say so and ask. If it lives in the display/configuration/profile space, propose freely.
